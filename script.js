const filters=document.querySelectorAll(".filter");const products=document.querySelectorAll(".product");const categoryLinks=document.querySelectorAll(".category[data-filter]");function applyFilter(value){products.forEach(product=>{product.classList.toggle("hidden",value!=="All"&&product.dataset.category!==value)});filters.forEach(btn=>btn.classList.toggle("active",btn.dataset.filter===value))}filters.forEach(btn=>btn.addEventListener("click",()=>applyFilter(btn.dataset.filter)));categoryLinks.forEach(link=>link.addEventListener("click",()=>applyFilter(link.dataset.filter)));const menu=document.querySelector(".menu-toggle");const nav=document.querySelector(".nav");menu?.addEventListener("click",()=>{nav.style.display=nav.style.display==="flex"?"none":"flex";nav.style.position="absolute";nav.style.top="78px";nav.style.left="0";nav.style.right="0";nav.style.padding="20px 6%";nav.style.background="var(--paper)";nav.style.flexDirection="column"});